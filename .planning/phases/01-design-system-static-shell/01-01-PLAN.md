---
phase: 01-design-system-static-shell
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - css/tokens.css
  - css/base.css
  - css/layout.css
  - css/utilities.css
  - pages/blog/index.html
  - pages/casos-de-exito/index.html
  - pages/capacitaciones/index.html
  - assets/icons/.gitkeep
  - assets/images/.gitkeep
autonomous: true
requirements:
  - DSYS-01
  - DSYS-02
  - DSYS-03
  - SCAL-01
  - SCAL-02
  - SCAL-03

must_haves:
  truths:
    - "All CSS custom properties are declared in tokens.css and can be verified with grep"
    - "The brand can be recolored by changing only the values in :root — no other CSS file hardcodes #B88A3B or #15181C"
    - "The directory structure includes pages/blog/, pages/casos-de-exito/, and pages/capacitaciones/ scaffolds"
    - "Typography scale (H1 clamp, H2 clamp, body 16px, label 14px) is declared in base.css"
    - "Section padding tokens (section-mobile: 64px, section-desktop: 96px) are applied in layout.css"
  artifacts:
    - path: "css/tokens.css"
      provides: "All CSS custom properties — colors, typography, spacing, shadows, border-radius"
      contains: "--color-carbon"
    - path: "css/base.css"
      provides: "CSS reset, Google Fonts @import, heading and body typography rules"
      contains: "font-display: swap"
    - path: "css/layout.css"
      provides: "Container max-width, section padding, grid helpers"
      contains: "--container-max"
    - path: "css/utilities.css"
      provides: "sr-only, btn base, section-header, visibility helpers"
      contains: ".sr-only"
    - path: "pages/blog/index.html"
      provides: "Scaffolding placeholder for future blog page"
      contains: "<!DOCTYPE html>"
    - path: "pages/casos-de-exito/index.html"
      provides: "Scaffolding placeholder for future casos page"
      contains: "<!DOCTYPE html>"
    - path: "pages/capacitaciones/index.html"
      provides: "Scaffolding placeholder for future capacitaciones page"
      contains: "<!DOCTYPE html>"
  key_links:
    - from: "css/tokens.css"
      to: "css/base.css, css/layout.css, and all future section CSS files"
      via: "@import or link tag — tokens must be loaded first"
      pattern: "--color-carbon|--color-gold-500"
---

<objective>
Create the CSS design token system and project directory scaffold that all subsequent plans depend on.

Purpose: Establish the single source of truth for colors, typography, spacing, and brand tokens. This is the foundation that allows every section to be built consistently and rebranded by editing one file.

Output:
- `css/tokens.css` — all CSS custom properties
- `css/base.css` — reset, font loading, typography rules
- `css/layout.css` — container, section spacing, responsive grid helpers
- `css/utilities.css` — sr-only, base button styles, section-header component
- `pages/*/index.html` — three scaffold placeholder pages
- `assets/icons/` and `assets/images/` directories
</objective>

<execution_context>
@/Users/juanjorquera/.claude/get-shit-done/workflows/execute-plan.md
@/Users/juanjorquera/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-design-system-static-shell/01-CONTEXT.md
@.planning/phases/01-design-system-static-shell/01-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create CSS tokens, base, layout and utilities files</name>
  <files>css/tokens.css, css/base.css, css/layout.css, css/utilities.css</files>
  <read_first>
    - .planning/phases/01-design-system-static-shell/01-UI-SPEC.md (canonical token values, type scale, spacing scale, component specs)
    - .planning/phases/01-design-system-static-shell/01-CONTEXT.md (locked decisions: palette hex values, typography choices)
  </read_first>
  <action>
Create four CSS files. Do not deviate from the exact values specified below — they come directly from the UI-SPEC and CONTEXT.md.

**css/tokens.css** — All CSS custom properties in `:root`:

```css
:root {
  /* === COLORS === */
  /* Dark surfaces */
  --color-carbon:        #15181C;
  --color-steel-dark:    #2A3038;
  --color-steel-mid:     #5E6873;

  /* Light surfaces */
  --color-warm-white:    #F5F5F2;
  --color-light-gray:    #E9ECEF;

  /* Accent */
  --color-gold-500:      #B88A3B;
  --color-gold-300:      #D6B15A;

  /* Semantic */
  --color-petroleum:     #1F4D4A;
  --color-white:         #FFFFFF;

  /* === TYPOGRAPHY === */
  --font-heading:        'Cormorant Garamond', Georgia, serif;
  --font-body:           'Inter', system-ui, sans-serif;

  /* Type scale — implement as rem (16px base) */
  --text-display:        clamp(2.5rem, 5vw, 4rem);    /* Hero H1: clamp(40px, 5vw, 64px) */
  --text-heading:        clamp(1.75rem, 3vw, 2.5rem); /* Section H2: clamp(28px, 3vw, 40px) */
  --text-body:           1rem;                         /* 16px */
  --text-label:          0.875rem;                     /* 14px */

  /* Line heights */
  --lh-display:          1.1;
  --lh-heading:          1.2;
  --lh-body:             1.6;
  --lh-label:            1.4;

  /* Font weights */
  --fw-regular:          400;
  --fw-semibold:         600;

  /* === SPACING SCALE === */
  --space-xs:            0.25rem;   /* 4px */
  --space-sm:            0.5rem;    /* 8px */
  --space-md:            1rem;      /* 16px */
  --space-lg:            1.5rem;    /* 24px */
  --space-xl:            2rem;      /* 32px */
  --space-2xl:           3rem;      /* 48px */
  --space-3xl:           4rem;      /* 64px */
  --space-section-mobile: 4rem;     /* 64px — section padding-block on mobile */
  --space-section-desktop: 6rem;    /* 96px — section padding-block on desktop */

  /* === LAYOUT === */
  --container-max:       75rem;     /* 1200px */
  --header-height:       4.5rem;    /* 72px — used for scroll-margin-top offset */

  /* === BORDERS === */
  --radius-sm:           2px;       /* Buttons */
  --radius-md:           4px;       /* Cards */

  /* === SHADOWS === */
  --shadow-card:         0 2px 8px rgba(0, 0, 0, 0.06);
  --shadow-card-hover:   0 6px 20px rgba(0, 0, 0, 0.10);
  --shadow-header:       0 2px 12px rgba(0, 0, 0, 0.40);

  /* === TRANSITIONS === */
  --transition-base:     all 0.25s ease;
  --transition-header:   background 0.3s ease, box-shadow 0.3s ease;

  /* === LOGO (rebrand-safe) === */
  --logo-accent-color:   var(--color-gold-500);
  --logo-text-color:     var(--color-white);
}
```

**css/base.css** — Reset + Google Fonts + typography rules:

```css
/* Google Fonts — Cormorant Garamond + Inter, weights 400 and 600 only */
@import url('https://fonts.googleapis.com/css2?family=Cormorant+Garamond:wght@400;600&family=Inter:wght@400;600&display=swap');

/* Modern CSS reset */
*, *::before, *::after {
  box-sizing: border-box;
  margin: 0;
  padding: 0;
}

html {
  font-size: 16px;
  scroll-behavior: smooth;
  -webkit-text-size-adjust: 100%;
}

body {
  font-family: var(--font-body);
  font-size: var(--text-body);
  font-weight: var(--fw-regular);
  line-height: var(--lh-body);
  color: var(--color-carbon);
  background-color: var(--color-warm-white);
  overflow-x: hidden;
}

/* Heading typography */
h1, h2, h3, h4, h5, h6 {
  font-family: var(--font-heading);
  font-weight: var(--fw-semibold);
  line-height: var(--lh-heading);
}

h1 {
  font-size: var(--text-display);
  line-height: var(--lh-display);
}

h2 {
  font-size: var(--text-heading);
}

h3 {
  font-family: var(--font-heading);
  font-size: 1.25rem;  /* 20px — presentational override for cards/pillars */
  font-weight: var(--fw-regular);
}

p {
  font-family: var(--font-body);
  font-size: var(--text-body);
  line-height: var(--lh-body);
}

a {
  color: inherit;
  text-decoration: none;
}

img, picture, video, canvas, svg {
  display: block;
  max-width: 100%;
}

input, button, textarea, select {
  font: inherit;
}

/* Smooth scroll margin offset for sticky header */
section[id] {
  scroll-margin-top: var(--header-height);
}

/* Remove animations for users who prefer reduced motion */
@media (prefers-reduced-motion: reduce) {
  *, *::before, *::after {
    animation-duration: 0.01ms !important;
    animation-iteration-count: 1 !important;
    transition-duration: 0.01ms !important;
  }
}
```

**css/layout.css** — Container, section spacing, grid helpers:

```css
/* Container utility */
.container {
  width: 100%;
  max-width: var(--container-max);
  margin-inline: auto;
  padding-inline: var(--space-lg);   /* 24px sides on mobile */
}

@media (min-width: 768px) {
  .container {
    padding-inline: var(--space-xl);  /* 32px sides on tablet+ */
  }
}

/* Section default padding */
section {
  padding-block: var(--space-section-mobile);  /* 64px mobile */
}

@media (min-width: 1024px) {
  section {
    padding-block: var(--space-section-desktop); /* 96px desktop */
  }
}

/* Section header component (reused across all 9 sections) */
.section-header {
  margin-bottom: var(--space-2xl);   /* 48px below header before content */
}

.section-header h2 {
  position: relative;
  display: inline-block;
}

.section-header h2::after {
  content: '';
  display: block;
  width: 48px;
  height: 3px;
  background-color: var(--color-gold-500);
  margin-top: 12px;
}

.section-header p {
  margin-top: var(--space-md);      /* 16px gap after H2+rule */
  font-size: var(--text-body);
  line-height: var(--lh-body);
}

/* Dark section text override */
.section--dark .section-header p,
.section--dark p {
  color: var(--color-steel-mid);
}

.section--dark .section-header h2,
.section--dark h2,
.section--dark h3 {
  color: var(--color-white);
}

/* Grid utility — 2 column default, collapses per section-specific CSS */
.grid-2 {
  display: grid;
  gap: var(--space-lg);             /* 24px */
  grid-template-columns: 1fr;
}

@media (min-width: 768px) {
  .grid-2 {
    grid-template-columns: repeat(2, 1fr);
  }
}

.grid-3 {
  display: grid;
  gap: var(--space-lg);
  grid-template-columns: 1fr;
}

@media (min-width: 768px) {
  .grid-3 {
    grid-template-columns: repeat(3, 1fr);
  }
}
```

**css/utilities.css** — Accessible helpers, button base styles, fade-in animation setup:

```css
/* Screen reader only */
.sr-only {
  position: absolute;
  width: 1px;
  height: 1px;
  padding: 0;
  margin: -1px;
  overflow: hidden;
  clip: rect(0, 0, 0, 0);
  white-space: nowrap;
  border: 0;
}

/* Button base — applies to all .btn variants */
.btn {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  gap: var(--space-sm);
  padding: 12px 28px;
  border-radius: var(--radius-sm);  /* 2px */
  font-family: var(--font-body);
  font-size: var(--text-label);     /* 14px */
  font-weight: var(--fw-semibold);  /* 600 */
  letter-spacing: 0.04em;
  line-height: var(--lh-label);
  cursor: pointer;
  border: none;
  text-decoration: none;
  transition: var(--transition-base);
  min-height: 44px;                 /* RESP-04: touch target minimum */
  white-space: nowrap;
}

/* Primary button — gold background, carbon text */
.btn--primary {
  background-color: var(--color-gold-500);
  color: var(--color-carbon);
  border: none;
}

.btn--primary:hover,
.btn--primary:focus-visible {
  background-color: var(--color-gold-300);
  transform: translateY(-1px);
}

/* Ghost button — transparent, white border (for dark backgrounds) */
.btn--ghost {
  background-color: transparent;
  color: var(--color-white);
  border: 1.5px solid var(--color-white);
}

.btn--ghost:hover,
.btn--ghost:focus-visible {
  background-color: rgba(255, 255, 255, 0.10);
}

/* Ghost dark button — transparent, carbon border (for light backgrounds) */
.btn--ghost-dark {
  background-color: transparent;
  color: var(--color-carbon);
  border: 1.5px solid var(--color-carbon);
}

.btn--ghost-dark:hover,
.btn--ghost-dark:focus-visible {
  background-color: rgba(21, 24, 28, 0.08);
}

/* Fade-in animation — triggered by IntersectionObserver adding .is-visible */
.fade-in {
  opacity: 0;
  transform: translateY(16px);
  transition: opacity 0.5s ease, transform 0.5s ease;
}

.fade-in.is-visible {
  opacity: 1;
  transform: translateY(0);
}

/* Visually hidden until JS loads — for progressive enhancement sections */
.js-only {
  display: none;
}
```
  </action>
  <verify>
    <automated>test -f "/Users/juanjorquera/Dropbox (Personal)/Sites/FN Mining Advisor/css/tokens.css" && grep -q "\-\-color-carbon" "/Users/juanjorquera/Dropbox (Personal)/Sites/FN Mining Advisor/css/tokens.css" && grep -q "\-\-color-gold-500" "/Users/juanjorquera/Dropbox (Personal)/Sites/FN Mining Advisor/css/tokens.css" && grep -q "font-display" "/Users/juanjorquera/Dropbox (Personal)/Sites/FN Mining Advisor/css/base.css" && grep -q "\-\-container-max" "/Users/juanjorquera/Dropbox (Personal)/Sites/FN Mining Advisor/css/layout.css" && grep -q "\.btn--primary" "/Users/juanjorquera/Dropbox (Personal)/Sites/FN Mining Advisor/css/utilities.css" && echo "PASS: all CSS files created with required tokens"</automated>
  </verify>
  <acceptance_criteria>
    - `css/tokens.css` exists and contains `--color-carbon: #15181C`
    - `css/tokens.css` contains `--color-gold-500: #B88A3B`
    - `css/tokens.css` contains `--color-gold-300: #D6B15A`
    - `css/tokens.css` contains `--container-max: 75rem`
    - `css/tokens.css` contains `--header-height: 4.5rem`
    - `css/tokens.css` contains `--logo-accent-color` (rebrand-safe token — DSYS-02)
    - `css/base.css` contains `font-display` (via Google Fonts @import URL with `display=swap`)
    - `css/base.css` contains `scroll-behavior: smooth`
    - `css/base.css` contains `scroll-margin-top: var(--header-height)`
    - `css/layout.css` contains `.section-header h2::after` with `width: 48px` and `height: 3px`
    - `css/utilities.css` contains `.btn--primary` with background referencing `--color-gold-500`
    - `css/utilities.css` contains `.fade-in` and `.fade-in.is-visible` classes
    - `css/utilities.css` contains `min-height: 44px` on `.btn` (touch target)
  </acceptance_criteria>
  <done>Four CSS files exist with all design tokens, base styles, layout helpers, and utility classes matching the UI-SPEC values exactly.</done>
</task>

<task type="auto">
  <name>Task 2: Create project directory scaffold and page stubs</name>
  <files>pages/blog/index.html, pages/casos-de-exito/index.html, pages/capacitaciones/index.html, assets/icons/.gitkeep, assets/images/.gitkeep</files>
  <read_first>
    - .planning/phases/01-design-system-static-shell/01-CONTEXT.md (directory structure specification under "Established Patterns")
    - .planning/REQUIREMENTS.md (SCAL-01, SCAL-02, SCAL-03)
  </read_first>
  <action>
Create the three scaffold page stubs and asset directory placeholders.

**pages/blog/index.html:**
```html
<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Artículos Técnicos — FN Mining Advisor</title>
  <link rel="stylesheet" href="../../css/tokens.css">
  <link rel="stylesheet" href="../../css/base.css">
</head>
<body>
  <main>
    <section style="min-height: 60vh; display: flex; align-items: center; justify-content: center; padding: 2rem;">
      <p style="font-family: Georgia, serif; color: #5E6873;">Página en construcción — Artículos Técnicos</p>
    </section>
  </main>
</body>
</html>
```

**pages/casos-de-exito/index.html:**
```html
<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Casos de Éxito — FN Mining Advisor</title>
  <link rel="stylesheet" href="../../css/tokens.css">
  <link rel="stylesheet" href="../../css/base.css">
</head>
<body>
  <main>
    <section style="min-height: 60vh; display: flex; align-items: center; justify-content: center; padding: 2rem;">
      <p style="font-family: Georgia, serif; color: #5E6873;">Página en construcción — Casos de Éxito</p>
    </section>
  </main>
</body>
</html>
```

**pages/capacitaciones/index.html:**
```html
<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Capacitaciones — FN Mining Advisor</title>
  <link rel="stylesheet" href="../../css/tokens.css">
  <link rel="stylesheet" href="../../css/base.css">
</head>
<body>
  <main>
    <section style="min-height: 60vh; display: flex; align-items: center; justify-content: center; padding: 2rem;">
      <p style="font-family: Georgia, serif; color: #5E6873;">Página en construcción — Capacitaciones</p>
    </section>
  </main>
</body>
</html>
```

For asset directories, create `.gitkeep` files inside `assets/icons/` and `assets/images/` so the directories are tracked by git:
- `assets/icons/.gitkeep` — empty file
- `assets/images/.gitkeep` — empty file
  </action>
  <verify>
    <automated>test -f "/Users/juanjorquera/Dropbox (Personal)/Sites/FN Mining Advisor/pages/blog/index.html" && test -f "/Users/juanjorquera/Dropbox (Personal)/Sites/FN Mining Advisor/pages/casos-de-exito/index.html" && test -f "/Users/juanjorquera/Dropbox (Personal)/Sites/FN Mining Advisor/pages/capacitaciones/index.html" && test -d "/Users/juanjorquera/Dropbox (Personal)/Sites/FN Mining Advisor/assets/icons" && test -d "/Users/juanjorquera/Dropbox (Personal)/Sites/FN Mining Advisor/assets/images" && echo "PASS: scaffold directories and page stubs exist"</automated>
  </verify>
  <acceptance_criteria>
    - `pages/blog/index.html` exists and contains `<!DOCTYPE html>`
    - `pages/casos-de-exito/index.html` exists and contains `<!DOCTYPE html>`
    - `pages/capacitaciones/index.html` exists and contains `<!DOCTYPE html>`
    - All three stub pages contain `lang="es"` on the html element
    - All three stub pages link to `../../css/tokens.css` and `../../css/base.css`
    - `assets/icons/` directory exists
    - `assets/images/` directory exists
  </acceptance_criteria>
  <done>Three scaffold pages exist at the specified paths. Asset directories are present. The SCAL-01/02/03 requirements are satisfied.</done>
</task>

</tasks>

<verification>
After both tasks complete, verify the foundation:

```bash
# Verify all CSS token values match UI-SPEC
grep -c "^  --" "/Users/juanjorquera/Dropbox (Personal)/Sites/FN Mining Advisor/css/tokens.css"
# Expected: 30+ custom property declarations

# Verify brand token rebrand safety (DSYS-02)
grep "logo-accent-color" "/Users/juanjorquera/Dropbox (Personal)/Sites/FN Mining Advisor/css/tokens.css"
# Expected: --logo-accent-color: var(--color-gold-500);

# Verify scaffold pages
ls "/Users/juanjorquera/Dropbox (Personal)/Sites/FN Mining Advisor/pages/"
# Expected: blog/  casos-de-exito/  capacitaciones/
```
</verification>

<success_criteria>
- `css/tokens.css` contains all 9 color tokens with exact hex values from UI-SPEC
- `css/tokens.css` contains typography, spacing, layout, shadow, and transition tokens
- `css/base.css` loads Cormorant Garamond + Inter from Google Fonts with `display=swap`
- `css/layout.css` implements `.section-header h2::after` gold rule (48px × 3px)
- `css/utilities.css` implements `.btn--primary`, `.btn--ghost`, `.btn--ghost-dark` with exact specs
- Three scaffold pages exist at `pages/blog/`, `pages/casos-de-exito/`, `pages/capacitaciones/`
- No color values are hardcoded outside `tokens.css` (everything else uses `var(--...)`)
</success_criteria>

<output>
After completion, create `.planning/phases/01-design-system-static-shell/01-01-SUMMARY.md` with:
- Files created and their purpose
- Token count in tokens.css
- Any deviations from UI-SPEC (there should be none)
- Status of each requirement addressed (DSYS-01, DSYS-02, DSYS-03, SCAL-01, SCAL-02, SCAL-03)
</output>
