---
phase: 1
slug: design-system-static-shell
status: draft
shadcn_initialized: false
preset: none
created: 2026-03-16
---

# Phase 1 — UI Design Contract

> Visual and interaction contract for Phase 1: Design System + Static Shell.
> Generated by gsd-ui-researcher, verified by gsd-ui-checker.

---

## Design System

| Property | Value | Source |
|----------|-------|--------|
| Tool | none — pure CSS custom properties | CONTEXT.md: greenfield vanilla HTML/CSS/JS, no build tool |
| Preset | not applicable | Stack decision: no shadcn, no component library |
| Component library | none — hand-authored BEM components | STACK.md: BEM for components, CSS custom properties for tokens |
| Icon library | Phosphor Icons or Heroicons (outline/regular variant, inline SVG) | CONTEXT.md: "iconos técnicos lineales del estilo de Phosphor Icons o Heroicons en variante outline/regular" |
| Heading font | Cormorant Garamond — weights 400, 600 — Google Fonts | CONTEXT.md canonical refs |
| Body/UI font | Inter — weights 400, 600 — Google Fonts | CONTEXT.md canonical refs; weight 500 dropped in favor of 600 |
| Font loading | `font-display: swap` mandatory on both font faces | REQUIREMENTS.md SEO-04, CONTEXT.md |

---

## Spacing Scale

Declared values (multiples of 4):

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Icon gaps, badge padding, divider spacing |
| sm | 8px | Inline element spacing, icon-to-label gap |
| md | 16px | Default padding inside cards and inputs |
| lg | 24px | Gap between grid items; nav link spacing |
| xl | 32px | Card padding (generous interior); footer columns gap |
| 2xl | 48px | Sub-section internal spacing; mobile section padding |
| 3xl | 64px | Mid-section vertical rhythm on tablet |
| section-mobile | 64px | `padding-block` on all `<section>` at <768px — CONTEXT.md said "60px mobile"; nearest multiple of 4 is 64px |
| section-desktop | 96px | `padding-block` on all `<section>` at ≥1024px (midpoint of 80–120px range) |

Exceptions:
- Touch targets minimum 44px (SEO-05 / RESP-04) — applies to all nav links, buttons, and mobile hamburger
- Hero height: 100vh desktop; `min-height: 100svh` on mobile to prevent content clipping
- Gold accent line under H2: 3px height × 48px width (decorative, not a spacing token)

---

## Typography

Two fonts, four canonical roles (plus one Display role for the hero), two weights. All sizes in `px` equivalents; implement as `rem` (`16px` base).

### Canonical Type Scale

| Role | Element | Font | Size | Weight | Line Height | Source |
|------|---------|------|------|--------|-------------|--------|
| Display | Hero H1 | Cormorant Garamond | `clamp(40px, 5vw, 64px)` | 600 (SemiBold) | 1.1 | CONTEXT.md: "100vh hero" + premium tone |
| Heading | Section H2 | Cormorant Garamond | `clamp(28px, 3vw, 40px)` | 600 (SemiBold) | 1.2 | Default heading — editorial weight |
| Sub-heading | Card H3, methodology step titles | Cormorant Garamond | 20px | 400 (Regular) | 1.3 | CONTEXT.md: methodology numbers large, Cormorant |
| Body | Paragraphs, card descriptions | Inter | 16px | 400 (Regular) | 1.6 | Readable body; 1.6 for long Spanish paragraphs |
| Label | Nav links, badges, CTA buttons, "Foco:" tag | Inter | 14px | 600 (SemiBold) | 1.4 | UI/interactive copy; slightly tighter; weight upgraded from 500 to 600 |

Declared weights: **400 (Regular)** and **600 (SemiBold)** — applied across both Cormorant Garamond and Inter. No third weight is declared in the type scale.

### Presentational Overrides (NOT part of the type scale)

These values are one-off visual treatments scoped to specific components. They do not add new type scale entries.

| Component | Element | Font | Size | Weight | Color | Notes |
|-----------|---------|------|------|--------|-------|-------|
| `.methodology-step` | Step numeral (01–05) | Cormorant Garamond | 56px | 400 | `--color-gold-500` | Decorative display numeral — evokes engineering report numbering |
| `.site-logo` | "FN" initials | Cormorant Garamond | 24px | 600 | `--color-gold-500` | Logo presentational — not a content heading |
| Mobile nav overlay | Nav links | Cormorant Garamond | 28px | 400 | `--color-white` | Full-screen overlay only — not reused elsewhere |

---

## Color

Full palette as CSS custom properties. The 60/30/10 split applies to the overall visual impression across the page, not to any single section.

### Palette Tokens

```css
:root {
  /* Dark surfaces */
  --color-carbon:        #15181C;  /* Primary dark bg: hero, metodología, innovación, CTA final */
  --color-steel-dark:    #2A3038;  /* Hero gradient endpoint */
  --color-steel-mid:     #5E6873;  /* Secondary text on dark, dividers on dark */

  /* Light surfaces */
  --color-warm-white:    #F5F5F2;  /* Propuesta de valor, Experiencia, Contacto bg */
  --color-light-gray:    #E9ECEF;  /* Servicios, Insights bg; card border */

  /* Accent */
  --color-gold-500:      #B88A3B;  /* Primary accent — see reserved list below */
  --color-gold-300:      #D6B15A;  /* Gold hover state and secondary gold usage */

  /* Semantic */
  --color-petroleum:     #1F4D4A;  /* Not used in Phase 1 — reserved for future badges */
  --color-white:         #FFFFFF;  /* Text on dark backgrounds; card backgrounds */
}
```

### 60/30/10 Distribution

| Role | Value | Distribution | Usage |
|------|-------|-------------|-------|
| Dominant (60%) | `--color-carbon` / `--color-warm-white` / `--color-light-gray` | Background of all 9 sections, alternating per rhythm below | Surfaces |
| Secondary (30%) | `--color-white` (#FFFFFF) | Cards, header (solid state), footer | Component surfaces |
| Accent (10%) | `--color-gold-500` `#B88A3B` | See reserved list | Accent only |
| Destructive | `#DC2626` (red-600) | Not used in Phase 1 | Reserved for Phase 4 form errors |

### Section Background Rhythm

Source: CONTEXT.md "Ritmo de color entre secciones"

| Section | Background | Tone |
|---------|------------|------|
| Hero | `linear-gradient(135deg, #15181C 0%, #2A3038 100%)` | Dark |
| Propuesta de Valor | `--color-warm-white` `#F5F5F2` | Light |
| Servicios | `--color-light-gray` `#E9ECEF` | Light |
| Innovación/Sustentabilidad | `--color-carbon` `#15181C` | Dark |
| Metodología | `--color-carbon` `#15181C` | Dark |
| Experiencia Profesional | `--color-warm-white` `#F5F5F2` | Light |
| Insights | `--color-light-gray` `#E9ECEF` | Light |
| CTA Final | `--color-carbon` `#15181C` | Dark |
| Contacto | `--color-warm-white` `#F5F5F2` | Light |

### Accent Reserved For

`--color-gold-500` (#B88A3B) is reserved exclusively for:

1. Primary CTA button background (text: `--color-carbon` for contrast)
2. Decorative gold line under each section H2 (3px × 48px, `::after` pseudo-element)
3. Methodology step numbers (01–05) — large Cormorant numerals (presentational override)
4. Left border accent on Propuesta de Valor pillar cards (3px `border-left`)
5. Logo "FN" typographic initials in the header (presentational override)
6. Scroll indicator chevron in hero
7. Hover state: top border on service cards (appears on `:hover`)

`--color-gold-300` (#D6B15A) is reserved for:
- Hover state of gold CTA button (background lightens)
- Trayectoria tagline separator line (1px, `--color-gold-300`)

### Text Colors

| Surface | Primary Text | Secondary Text |
|---------|-------------|----------------|
| Dark sections | `--color-white` `#FFFFFF` | `--color-steel-mid` `#5E6873` |
| Light sections | `--color-carbon` `#15181C` | `#5E6873` (same mid-gray) |
| Cards (white bg) | `--color-carbon` `#15181C` | `#5E6873` |

---

## Components

Phase 1 introduces these base components (documented as BEM blocks):

### `.btn`

| Variant | Background | Text | Border | Hover |
|---------|-----------|------|--------|-------|
| `.btn--primary` | `--color-gold-500` | `--color-carbon` | none | bg → `--color-gold-300`, `transform: translateY(-1px)` |
| `.btn--ghost` | transparent | `--color-white` | 1.5px `--color-white` | bg → `rgba(255,255,255,0.1)` |
| `.btn--ghost-dark` | transparent | `--color-carbon` | 1.5px `--color-carbon` | bg → `rgba(21,24,28,0.08)` |

Sizing: `padding: 12px 28px`, `border-radius: 2px`, `font: Inter 14px weight 600`, `letter-spacing: 0.04em`.

Transition on all interactive elements: `transition: all 0.25s ease`.

### `.service-card`

- Background: `--color-white`
- Border: 1px `--color-light-gray`
- Box-shadow: `0 2px 8px rgba(0,0,0,0.06)`
- Padding: 32px (xl token)
- Border-radius: 4px
- Hover: `transform: translateY(-2px)`, shadow `0 6px 20px rgba(0,0,0,0.10)`, `border-top: 2px solid --color-gold-500`
- Transition: `0.25s ease`

Contents order: SVG icon (32px, `--color-carbon`) → H3 (service title) → description paragraph → "Foco:" label + inline terms.

### `.section-header`

Reusable header for each section:
- H2 (Cormorant Garamond, `clamp(28px, 3vw, 40px)`, weight 600)
- `::after` pseudo-element: `display: block; width: 48px; height: 3px; background: --color-gold-500; margin-top: 12px`
- Optional subheadline paragraph (Inter 16px weight 400, secondary text color)

### `.methodology-step`

Presentational overrides apply here (see Typography section):
- Step number: Cormorant Garamond, **56px** (presentational override), weight 400, `--color-gold-500`
- Step title: Inter, 16px, weight 600 (Label role)
- Step body: Inter, 16px, weight 400 (Body role)
- Layout: number left/top, content right/below
- Connector: vertical or horizontal line `1px --color-steel-mid` opacity 0.3

### `.trust-pillar`

- Border-left: 3px solid `--color-gold-500`
- Padding-left: 20px
- Title: Cormorant Garamond 20px weight 400 (Sub-heading role)
- Body: Inter 16px weight 400 (Body role)

### Header

| State | Background | Logo | Nav Links | CTA Button |
|-------|-----------|------|-----------|------------|
| Over hero (default) | transparent | "FN" gold + "Mining Advisor" white | `--color-white` | `.btn--ghost` |
| Scrolled past hero | `--color-carbon` + `box-shadow: 0 2px 12px rgba(0,0,0,0.4)` | same | `--color-white` | `.btn--primary` |

Transition: `background 0.3s ease, box-shadow 0.3s ease`.

Logo typographic treatment (presentational override — not type scale):
- "FN" — Cormorant Garamond, **24px**, weight 600, color `--color-gold-500`
- " Mining Advisor" — Inter, 14px, weight 400, color `--color-white`

Mobile hamburger: 3-bar icon (24px), `--color-white`. Requires `aria-label="Abrir menú"` in closed state; toggle to `aria-label="Cerrar menú"` when open. Opens full-screen overlay with `--color-carbon` background. Nav links in overlay use the mobile nav presentational override: Cormorant Garamond **28px**, weight 400, centered. CTA button centered below links. Animación de apertura: fade-in (`opacity: 0 → 1`) 0.2s ease.

---

## Animations and Interactions

Source: REQUIREMENTS.md UX-01, UX-02, UX-03; CONTEXT.md.

| Interaction | Implementation | Duration | Notes |
|-------------|----------------|----------|-------|
| Fade-in on scroll | `IntersectionObserver` — adds `.is-visible` class; CSS `opacity: 0 → 1` + `transform: translateY(16px) → 0` | 0.5s ease | No external library; threshold 0.15 |
| Card hover lift | CSS `transform: translateY(-2px)` + shadow amplification | 0.25s ease | Applied via `:hover` pseudo-class |
| Button hover | Background color shift + `translateY(-1px)` | 0.25s ease | All `.btn` variants |
| Hero scroll indicator | CSS `@keyframes bounce` — `translateY(0 → 6px → 0)` | 1.5s ease-in-out infinite | Pure CSS, no JS |
| Header solidify | `scroll` event or `IntersectionObserver` on hero — class toggle | 0.3s ease | Detects scroll past hero height |
| Smooth anchor scroll | `scroll-behavior: smooth` on `html` element + `scroll-margin-top: 72px` on each section | native CSS | 72px = sticky header height |
| Mobile nav open/close | Class toggle `.nav--open` — CSS `opacity: 0 → 1` + `visibility` | 0.2s ease | fade-in overlay; toggle `aria-label` on hamburger button |

No parallax, no particle effects, no complex SVG animations (out of scope per requirements).

---

## Copywriting Contract

All copy in Spanish. Mining/metallurgical terminology required (NI 43-101, flotación, conminución, recuperación metalúrgica, etc.).

| Element | Copy | Notes |
|---------|------|-------|
| Primary CTA (header + hero) | "Solicitar diagnóstico" | CONTEXT.md: "Nav desktop: botón CTA 'Solicitar diagnóstico'" |
| Secondary CTA (hero) | "Ver servicios" | Ghost button, scrolls to #servicios |
| Hero H1 (placeholder) | "Criterio técnico senior para decisiones mineras críticas" | Establishes authority; replaces with WP content in Phase 4 |
| Hero subheadline (placeholder) | "Consultoría especializada en valorización de propiedades, optimización metalúrgica y gestión de proyectos mineros en Chile y Latinoamérica." | Technical + geographic scope |
| Trayectoria tagline | "+20 años de experiencia en minería de oro y cobre, Chile y Latinoamérica" | CONTEXT.md verbatim |
| CTA Final primary button | "Solicitar diagnóstico inicial" | More specific than header CTA — conversion moment |
| CTA Final secondary button | "Consultar por WhatsApp" | REQUIREMENTS.md CONT-03 |
| Contact form submit button | "Enviar consulta" | Descriptive action |
| Confidentiality note | "Sus datos y consulta son tratados con estricta confidencialidad" | REQUIREMENTS.md CONT-02 verbatim |
| Insights empty state heading | "Próximamente: Artículos técnicos" | REQUIREMENTS.md API-05: "estado 'próximamente'" |
| Insights empty state body | "Publicaremos artículos de análisis sobre optimización metalúrgica, mercados mineros y mejores prácticas del sector." | Anticipates future content; no call to action |
| Form error state | "No fue posible enviar su consulta. Verifique su conexión e intente nuevamente, o contáctenos directamente por email." | Problem + fallback path (Phase 4 — contract defined here for static placeholder) |
| Form success state | "Su consulta fue enviada exitosamente. Le responderemos dentro de 24–48 horas hábiles." | Phase 4 active; static shell shows no state |
| Destructive actions | None in Phase 1 | No delete/remove actions in static shell |

---

## Responsive Breakpoints

| Breakpoint | Width | Notes |
|-----------|-------|-------|
| Mobile | 320px – 767px | Base styles (mobile-first) |
| Tablet | 768px – 1023px | Services: 2-col grid |
| Desktop | 1024px+ | Services: 2×2 grid; full nav visible |
| Wide | 1200px max-width | `--container-max: 1200px` — content centered |

Services grid: 1 col (mobile) → 2 col (tablet+). Source: CONTEXT.md "Grid: 2 columnas × 2 filas en desktop; 1 columna en mobile; 2 columnas en tablet."

---

## Registry Safety

Not applicable. This project uses no component registry, no npm packages, and no shadcn. All components are hand-authored vanilla HTML/CSS. No third-party code blocks are introduced in Phase 1.

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | none — not initialized | not applicable |
| Third-party registries | none | not applicable |

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
